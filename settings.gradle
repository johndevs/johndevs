plugins {
    id 'com.devsoap.cache' version '1.2.5'
}

devsoap {
    email = System.getenv("DEVSOAP_GRADLE_CACHE_EMAIL")
    key = System.getenv("DEVSOAP_GRADLE_CACHE_KEY")
}

buildCache {
    local {
        enabled = false
    }
}

rootProject.name = 'johndevs'

